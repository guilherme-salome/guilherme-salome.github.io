# Copy to .env and fill in values.
#
# IMPORTANT: Do NOT point this at your entire org-roam unless you understand the
# exporter rules. By default it only exports files tagged :publish: or containing
# `#+PUBLISH: true`.

# Directory containing org files to export
ORG_ROAM_DIR=/path/to/your/org

# Where generated markdown should go (Astro content collection)
ORG_OUT_DIR=src/content/writing

# Tag required for export (used in #+FILETAGS:, e.g. :publish:causal:)
ORG_PUBLISH_TAG=publish

# Optional
# ORG_EXT=.org
